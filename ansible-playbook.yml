- name: Start a mysql container and link
  hosts: test
  docker_container:
    name: mysql
    image: mysql
    networks:
      - name: Link MySQL container
        links:
          - jenkins:jenkins
        
      
